---
title: T0.8 — SBT Deprovision Evidence (usbasicfoundationtenant)
date: 2026-05-07
build: deprovisioningScriptJobcode-A1OuMNKRPgHF:d94f5fe1-5dbc-4b14-a101-b3c5eb50abf7
duration: 117 seconds (16:18:10 → 16:20:08 -05:00)
status: SUCCEEDED (per CodeBuild status); INCOMPLETE (per actual end-state)
---

# T0.8 — SBT Deprovision Evidence Capture

**Tenant**: `usbasicfoundationtenant` (`34392ed6-2e51-4fc8-ae2c-242eb5710e40`, GENERIC, USA)

## Pre/Post DDB Diff

| Resource | Pre | Post | SBT Action | Status |
|---|---:|---:|---|---|
| edforge-identity-basic | 7 | **1** | Attempted delete-by-PK | ⚠️ **1 survivor** (SBT bug) |
| edforge-academics-basic | 0 | 0 | Attempted | ✓ no-op |
| edforge-finance-basic | 0 | 0 | Attempted | ✓ no-op |
| TenantMappingTable | 0 | 0 | Not touched | ✓ (correct — not per-tenant) |
| controlplane mgmt table | 1 | **1** | Marked `sbtaws_active: false` | ✓ audit row preserved |
| edforge-analytics | 28 | **28** | Not touched | ❌ orphan |
| edforge-analytics-landing | 7 | **7** | Not touched | ❌ orphan |
| controlplane reg table | 1 (status=Created) | **1 (status=Deleted, sbtaws_active=false)** | Updated | ✓ audit row preserved |

| Resource | Pre | Post | SBT Action | Status |
|---|---|---|---|---|
| Cognito group `34392ed6-...` | exists, 1 user | **GROUP_MISSING** | Deleted users + group | ✓ |
| SNS `edforge-alerts-tenant-34392ed6-...` | exists | **exists** | Not touched | ❌ orphan |

## SBT Bug — `delete_items_if_exists` word-splitting

[deprovision-tenant.sh:76-78](../../../server/lib/provision-scripts/deprovision-tenant.sh#L76-L78):

```bash
for ITEM in $(echo "$QUERY_OUTPUT" | jq -c '.Items[]'); do
  ITEM_KEY=$(echo "$ITEM" | jq -r '.'$PARTITION_KEY_NAME'.S')
  ITEM_SORT_KEY=$(echo "$ITEM" | jq -r '.'$SORT_KEY_NAME'.S')
```

`for ... in $(...)` word-splits on whitespace, including whitespace **inside** JSON strings (gradeLevels list, address map, etc.). When jq emits a complex item, the loop receives mangled fragments instead of one item per iteration. Two outcomes:
1. Mangled fragments fail jq parsing → empty `ITEM_KEY` / `ITEM_SORT_KEY` → AWS rejects with `ValidationException: AttributeValue for a key attribute cannot contain an empty string value`
2. The script logs `Deleted item with tenantId =  and entityKey =` even though the API call failed — **misleading log output**.

CloudWatch log evidence (filtered):

```
aws: [ERROR]: An error occurred (ValidationException) when calling the DeleteItem operation:
  One or more parameter values are not valid. The AttributeValue for a key attribute cannot
  contain an empty string value. Key: tenantId
Deleted item with tenantId =  and entityKey =       (← lying about success)
```

**Survivor row in identity-basic**:

```
entityType: SCHOOL
entityKey: SCHOOL#8bba570e-53d6-4b5e-98fe-bbef4418b70c
schoolName: West Park High School
gradeRange: { start: "5", end: "12" }   ← complex attribute that broke jq word-splitting
gradeLevels: [FifthGrade, SixthGrade, ..., TwelfthGrade]
address: { street1, city, state, zipCode, country }
```

The simpler keys (`SETTINGS#WORKSPACE`, `USER#<id>`, `USER#<id>#PREFS`, `SCHOOL#...#CONFIG`) survived word-splitting OK. The full SCHOOL row with map/list attributes did not.

## Implications for plan

### Sprint 0.5 T0.9 redesign

Original assumption was "SBT covers identity/academics/finance; we gap-fill analytics + SNS." Now revised:

- **T0.9a expanded** to defensively re-sweep ALL 5 tenant-data tables (identity, academics, finance, analytics, analytics-landing) — must catch SBT survivors in addition to gap tables. This is the right defensive posture: the cleanup script trusts nothing.
- **T0.9b** unchanged — per-tenant SNS topic deletion.
- **T0.9c** revised — read-only verifier that confirms SBT control plane rows are flagged `sbtaws_active=false` and `registrationStatus="Deleted"`. Don't delete; SBT pattern is "preserve audit row."
- **T0.9d** — orchestrator composing 0.9a + 0.9b + 0.9c with Saraswati refuse-list + whitelist.

### Sprint 5 long-term — fix the SBT script

The bash word-splitting bug should be fixed in `deprovision-tenant.sh`. Two-line patch:

```bash
# Before (broken):
for ITEM in $(echo "$QUERY_OUTPUT" | jq -c '.Items[]'); do

# After (correct):
echo "$QUERY_OUTPUT" | jq -c '.Items[]' | while IFS= read -r ITEM; do
```

Add to Sprint 5 backlog as a follow-up. Until that ships, the CLI deprovision must defensively re-sweep.

## SBT post-state convention — confirmed

SBT preserves the registration + management rows as audit history with two flags:
- `sbtaws_active: false` (BOOL)
- `registrationStatus: "Deleted"` (S)

This is the right pattern: lifecycle history is preserved, but the row no longer counts as an active tenant. **Sprint 5's lifecycle-status endpoint can use these flags as the source of truth for "is this tenant deprovisioned."**

## CodeBuild duration

Wall clock 117 seconds for a tenant with 7 identity rows + 1 user + minimal academics/finance. For larger tenants like `rainshoaiborg` (8,023 rows), expect proportionally longer — but the script is mostly dominated by setup overhead (CDK install, AWS calls), not the actual delete loop. Conservative SBT-job timeout for Sprint 5 T5.11: **15 minutes** is correct.
