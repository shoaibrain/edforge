---
title: T0.7 — Pre-Cleanup Resource Inventory
date: 2026-05-07
status: Snapshot complete; informs Sprint 0.5 cleanup script design
---

# T0.7 — Pre-Cleanup Resource Inventory (all 4 tenants)

Read-only snapshot taken before any cleanup. Saraswati included for reference (will not be touched).

## Row counts per tenant per table

| Table | usbasic… | rainshoaiborg | prodtestadmin | **Saraswati (preserve)** |
|---|---:|---:|---:|---:|
| `edforge-identity-basic` | 7 | 412 | 799 | **6** |
| `edforge-academics-basic` | 0 | 3,417 | 964 | **0** |
| `edforge-finance-basic` | 0 | 504 | 96 | **0** |
| `shared-infra-stack-TenantMappingTable*` | 0 | 0 | 0 | **0** |
| `controlplane-stack-…-tenantManagementTable*` (SBT) | 1 | 1 | 1 | **1** |
| `edforge-analytics` | 28 | 171 | 325 | **37** |
| `edforge-analytics-landing` (matched/4688 scanned) | 7 | 3,517 | 1,161 | **3** |
| `controlplane-stack-…-tenantRegistrationTable*` (SBT) | 1 | 1 | 1 | **1** |
| **Total DDB rows** | **44** | **8,023** | **3,347** | **48** |

## Cognito state (shared pool `ap-south-1_spYeNvNJt`)

| Tenant | Group exists? | Users in group |
|---|---|---|
| usbasic… | ✅ | 1 |
| rainshoaiborg | ✅ | 4 |
| prodtestadmin | ✅ | 2 |
| **Saraswati** | ✅ | **1** |

## Per-tenant SNS topics

Naming pattern: `edforge-alerts-tenant-<tenantId>` (corrected from initial assumption of `edforge-tenant-<tenantId>-alerts`).

| Tenant | Topic ARN |
|---|---|
| usbasic… | `arn:aws:sns:ap-south-1:257526644020:edforge-alerts-tenant-34392ed6-2e51-4fc8-ae2c-242eb5710e40` |
| rainshoaiborg | `arn:aws:sns:ap-south-1:257526644020:edforge-alerts-tenant-fc9ea1c1-1cc2-45b3-b8c4-7e953e8e30d7` |
| prodtestadmin | **NONE** (likely provisioned before SNS-topic feature was added) |
| **Saraswati** | `arn:aws:sns:ap-south-1:257526644020:edforge-alerts-tenant-34f49822-ae1d-4188-95f0-04e14bc6c662` (preserve) |

## Operator-level SNS topics (preserve always)

These are NOT per-tenant; they fire on alarm/provisioning failures and must survive Sprint 0.5:

- `edforge-alerts-operator`
- `edforge-provisioning-alerts`

The cleanup scripts (T0.9b) MUST refuse to delete any topic whose name doesn't match `edforge-alerts-tenant-<UUID>`.

## Plan corrections from this inventory

1. **T0.9c (tenant-mapping registration cleanup) — DROPPED.** TenantMappingTable holds tier→stack mappings (rows keyed by literal `basic` and `advanced`), not per-tenant data. The 0-count for all 4 tenants confirms this. The 2 items in the table are framework infrastructure that must be preserved.

2. **T0.9c — REPLACED with SBT control plane row cleanup.** Each tenant has 1 row in `tenantManagementTable` and 1 row in `tenantRegistrationTable`. SBT's `deprovision-tenant.sh` doesn't touch these. Whether they should be **deleted** vs **marked inactive** (`sbtaws_active=false`) is TBD pending T0.8 evidence — observe what state these rows are in after the SBT job runs against `usbasic…`.

3. **T0.9b idempotency** — `prodtestadmin` has no SNS topic. Cleanup script must silently no-op when the topic is missing.

4. **rainshoaiborg is by far the largest test tenant** (8,023 DDB rows). It exercises the gap-fill scripts at scale; useful as the second cleanup target after `usbasic` validates the flow.

## Saraswati's footprint is tiny

Saraswati has 48 DDB rows total (6 identity + 37 analytics + 3 landing + 1 management + 1 registration + 0 in others). Sprint 1's tenantTag backfill is a single UpdateItem on the METADATA row. No batch-update script needed; can be done at the keyboard with you.

## Resource summary (before any cleanup)

- **3 test tenants** total: 11,414 DDB rows + 3 Cognito groups (7 users) + 2 SNS topics
- **Saraswati**: 48 DDB rows + 1 Cognito group (1 user) + 1 SNS topic — DO NOT TOUCH
- **2 framework rows** in TenantMappingTable (basic + advanced tier mappings) — preserve
- **2 operator SNS topics** — preserve

## Cost (read-only inventory)

- DDB query/scan: ~$0.01 (3 tenants × 7 tables × ~5KB read each)
- Cognito list-users-in-group: ~free
- SNS list-topics: free
- Total: <$0.05
- Wall-clock: 67 seconds

## Next: T0.8

Trigger SBT `deprovision-tenant.sh` against `usbasicfoundationtenant` (smallest test tenant, GENERIC archetype = farthest from Saraswati's PABSON). Capture pre/post state diff to confirm:
- Cognito group + 1 user → deleted by SBT
- 7 identity rows → deleted by SBT
- 0 academics + 0 finance rows → no-op
- 28 analytics rows → **expected orphan** (gap-fill target)
- 7 landing rows → **expected orphan**
- 1 management table row + 1 registration row → state TBD (deleted vs flagged inactive)
- SNS topic → **expected orphan**
