# Authorization Coverage — generated map

> Generated by `scripts/audit/authz-coverage.ts`. Do not edit by hand.

**Totals:** 374 routes — authz 244, internal 2, **authn-only 125**, **public 3**.

## REVIEW — routes without resource authorization

| Class | Route | Guards | Note |
|---|---|---|---|
| authn-only | `DELETE /calendar-blocks/:blockId` | JwtAuthGuard |  |
| authn-only | `DELETE /schools/:schoolId/academic-sessions/:sessionId` | JwtAuthGuard |  |
| authn-only | `DELETE /schools/:schoolId/academic-years/:yearId/holidays/:holidayId` | JwtAuthGuard |  |
| authn-only | `DELETE /sessions/:id` | JwtAuthGuard |  |
| authn-only | `DELETE /staff/:staffId` | JwtAuthGuard |  |
| authn-only | `DELETE /staff/:staffId/assignments/:assignmentId` | JwtAuthGuard |  |
| authn-only | `DELETE /staff/:staffId/credentials/:credentialId` | JwtAuthGuard |  |
| authn-only | `DELETE /staff/:staffId/trainings/:trainingId` | JwtAuthGuard |  |
| authn-only | `DELETE /users/:id/roles/:schoolId` | JwtAuthGuard |  |
| authn-only | `DELETE /users/:userId/security/sessions/:sessionId` | JwtAuthGuard |  |
| authn-only | `GET /academics/dashboard/overview` | JwtAuthGuard |  |
| authn-only | `GET /archetype-defaults` | JwtAuthGuard |  |
| public | `GET /auth/health` | — |  |
| authn-only | `GET /auth/me` | JwtAuthGuard |  |
| authn-only | `GET /calendar-blocks` | JwtAuthGuard |  |
| authn-only | `GET /calendar-blocks/:blockId` | JwtAuthGuard |  |
| authn-only | `GET /credentials/expiring` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/escs` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/escs/:escId` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/hierarchy` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/leas` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/leas/:leaId` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/networks` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/networks/:networkId` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/networks/:networkId/members` | JwtAuthGuard |  |
| authn-only | `GET /education-organizations/sea` | JwtAuthGuard |  |
| authn-only | `GET /finance/payments/verify/:sessionId` | JwtAuthGuard |  |
| authn-only | `GET /holiday-seeds` | JwtAuthGuard |  |
| authn-only | `GET /reporting/snapshots` | JwtAuthGuard |  |
| authn-only | `GET /reporting/snapshots/:snapshotId` | JwtAuthGuard |  |
| authn-only | `GET /reporting/snapshots/:snapshotId/download` | JwtAuthGuard |  |
| authn-only | `GET /school-years` | JwtAuthGuard |  |
| authn-only | `GET /school-years/current` | JwtAuthGuard |  |
| authn-only | `GET /school-years/current-all` | JwtAuthGuard |  |
| authn-only | `GET /schools` | JwtAuthGuard, GlobalRoleGuard | authz guard present but NO @RequirePermission (no-op) |
| authn-only | `GET /schools/:schoolId` | JwtAuthGuard, GlobalRoleGuard | authz guard present but NO @RequirePermission (no-op) |
| authn-only | `GET /schools/:schoolId/academic-sessions` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/academic-sessions/:sessionId` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/academic-years` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/academic-years/:yearId` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/academic-years/:yearId/grading-periods` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/academic-years/:yearId/holidays` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/academic-years/current` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/activation-requirements` | JwtAuthGuard, GlobalRoleGuard | authz guard present but NO @RequirePermission (no-op) |
| authn-only | `GET /schools/:schoolId/branding` | JwtAuthGuard, PermissionGuard | authz guard present but NO @RequirePermission (no-op) |
| authn-only | `GET /schools/:schoolId/configuration` | JwtAuthGuard, GlobalRoleGuard | authz guard present but NO @RequirePermission (no-op) |
| authn-only | `GET /schools/:schoolId/departments` | JwtAuthGuard, GlobalRoleGuard | authz guard present but NO @RequirePermission (no-op) |
| authn-only | `GET /schools/:schoolId/departments/:departmentId` | JwtAuthGuard, GlobalRoleGuard | authz guard present but NO @RequirePermission (no-op) |
| authn-only | `GET /schools/:schoolId/shift-profile` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/staff` | JwtAuthGuard |  |
| authn-only | `GET /schools/:schoolId/users` | JwtAuthGuard |  |
| authn-only | `GET /sessions` | JwtAuthGuard |  |
| authn-only | `GET /sessions/:id` | JwtAuthGuard |  |
| authn-only | `GET /sessions/user/:userId` | JwtAuthGuard |  |
| authn-only | `GET /staff` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/assignments` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/assignments/:assignmentId` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/credentials` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/credentials/:credentialId` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/employment-history` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/leave` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/leave/:leaveId` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/trainings` | JwtAuthGuard |  |
| authn-only | `GET /staff/:staffId/trainings/:trainingId` | JwtAuthGuard |  |
| authn-only | `GET /staff/by-email` | JwtAuthGuard |  |
| authn-only | `GET /staff/search/:term` | JwtAuthGuard |  |
| authn-only | `GET /tenants/:tenantId` | JwtAuthGuard |  |
| public | `GET /tenants/lookup` | — |  |
| authn-only | `GET /tenants/my/settings` | JwtAuthGuard |  |
| authn-only | `GET /users/:id` | JwtAuthGuard |  |
| authn-only | `GET /users/:id/assignments` | JwtAuthGuard |  |
| authn-only | `GET /users/:id/display-name` | JwtAuthGuard |  |
| authn-only | `GET /users/:id/preferences` | JwtAuthGuard |  |
| authn-only | `GET /users/:id/roles` | JwtAuthGuard |  |
| authn-only | `GET /users/:id/roles/:schoolId` | JwtAuthGuard |  |
| authn-only | `GET /users/:id/roles/permissions/catalog` | JwtAuthGuard |  |
| authn-only | `GET /users/:userId/security` | JwtAuthGuard |  |
| authn-only | `GET /users/:userId/security/login-history` | JwtAuthGuard |  |
| authn-only | `GET /users/:userId/security/sessions` | JwtAuthGuard |  |
| authn-only | `GET /users/me` | JwtAuthGuard |  |
| authn-only | `GET /users/me/permissions` | JwtAuthGuard |  |
| authn-only | `PATCH /calendar-blocks/:blockId` | JwtAuthGuard |  |
| authn-only | `PATCH /reporting/snapshots/:snapshotId/transition` | JwtAuthGuard |  |
| authn-only | `PATCH /schools/:schoolId/academic-sessions/:sessionId` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/assignments/:assignmentId` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/credentials/:credentialId` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/credentials/:credentialId/verify` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/employment-status` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/leave/:leaveId/approve` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/leave/:leaveId/cancel` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/leave/:leaveId/reject` | JwtAuthGuard |  |
| authn-only | `PATCH /staff/:staffId/trainings/:trainingId` | JwtAuthGuard |  |
| authn-only | `PATCH /users/:id` | JwtAuthGuard |  |
| authn-only | `PATCH /users/:id/preferences` | JwtAuthGuard |  |
| authn-only | `PATCH /users/:id/roles/:schoolId` | JwtAuthGuard |  |
| public | `POST /auth/login` | — |  |
| authn-only | `POST /auth/logout` | JwtAuthGuard |  |
| authn-only | `POST /auth/refresh` | JwtAuthGuard |  |
| authn-only | `POST /calendar-blocks` | JwtAuthGuard |  |
| authn-only | `POST /reporting/snapshots` | JwtAuthGuard |  |
| authn-only | `POST /reporting/snapshots/preflight` | JwtAuthGuard |  |
| authn-only | `POST /schools/:schoolId/academic-sessions` | JwtAuthGuard |  |
| authn-only | `POST /schools/:schoolId/academic-years` | JwtAuthGuard |  |
| authn-only | `POST /schools/:schoolId/academic-years/:yearId/grading-periods` | JwtAuthGuard |  |
| authn-only | `POST /schools/:schoolId/academic-years/:yearId/holidays` | JwtAuthGuard |  |
| authn-only | `POST /schools/:schoolId/staff` | JwtAuthGuard |  |
| authn-only | `POST /sessions/revoke-all` | JwtAuthGuard |  |
| authn-only | `POST /sessions/user/:userId/revoke-all` | JwtAuthGuard |  |
| authn-only | `POST /staff` | JwtAuthGuard |  |
| authn-only | `POST /staff/:staffId/assignments` | JwtAuthGuard |  |
| authn-only | `POST /staff/:staffId/credentials` | JwtAuthGuard |  |
| authn-only | `POST /staff/:staffId/leave` | JwtAuthGuard |  |
| authn-only | `POST /staff/:staffId/trainings` | JwtAuthGuard |  |
| authn-only | `POST /staff/with-user` | JwtAuthGuard |  |
| authn-only | `POST /users/:id/roles` | JwtAuthGuard |  |
| authn-only | `POST /users/:id/roles/:schoolId/change` | JwtAuthGuard |  |
| authn-only | `POST /users/:id/roles/permissions/check` | JwtAuthGuard |  |
| authn-only | `POST /users/:userId/security/change-password` | JwtAuthGuard |  |
| authn-only | `POST /users/:userId/security/mfa/disable` | JwtAuthGuard |  |
| authn-only | `POST /users/:userId/security/mfa/setup` | JwtAuthGuard |  |
| authn-only | `POST /users/:userId/security/mfa/verify` | JwtAuthGuard |  |
| authn-only | `POST /users/:userId/security/sessions/revoke-all` | JwtAuthGuard |  |
| authn-only | `PUT /schools/:schoolId/academic-years/:yearId` | JwtAuthGuard |  |
| authn-only | `PUT /schools/:schoolId/academic-years/:yearId/grading-periods/:termId` | JwtAuthGuard |  |
| authn-only | `PUT /schools/:schoolId/academic-years/:yearId/set-current` | JwtAuthGuard |  |
| authn-only | `PUT /schools/:schoolId/academic-years/:yearId/status` | JwtAuthGuard |  |

## Full map

### academics

| Route | Class | Authz | Guards |
|---|---|---|---|
| `DELETE /academics/classwork/:itemId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/classwork/topics/:topicId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/course-offerings/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/courses/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/exams/:examId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/exams/:examId/courses/:examCourseId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/promotion-rules/:ruleId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/sections/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/sections/:id/students/:studentId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /academics/students/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/alerts` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/overview` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/policy` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/student-trends` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/student/:studentId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/student/:studentId/summary` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/summary` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/attendance/trend` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/board-exams` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/classwork` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/course-offerings` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/course-offerings/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/courses` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/courses/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/dashboard/overview` | authn-only | — | JwtAuthGuard |
| `GET /academics/exams` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/exams/:examId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/exams/:examId/courses` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/exams/:examId/courses/:examCourseId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/exams/:examId/scores` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/exams/:examId/scores/:scoreId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/exams/exam-pattern` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/grades` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/grades/overview` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/grades/section/:sectionId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/grading-policies` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/grading-policies/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/promotion-rules` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/promotion-rules/:ruleId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/result-cards` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/result-cards/:cardId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/schools/:schoolId/academic-years/:yearId/calendars` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/schools/:schoolId/years/:yearId/enrollments` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/schools/:schoolId/years/:yearId/enrollments/export` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/schools/:schoolId/years/:yearId/enrollments/summary` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/schools/:schoolId/years/:yearId/students/:studentId/enrollment` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/schools/:schoolId/years/:yearId/students/:studentId/student-school-association` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/section-attendance` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/section-attendance/student/:studentId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/sections` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/sections/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/sections/:id/students` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:id/attendance` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:id/attendance/summary` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:id/enrollments` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:id/grades` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:id/profile` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:id/sections` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:studentId/enrollment` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:studentId/student-school-associations` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/:studentId/timeline` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/check-duplicate` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/import/iemis/jobs` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /academics/students/import/iemis/jobs/:jobId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/attendance/:date/:studentId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/classwork/:itemId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/classwork/reorder` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/classwork/topics/:topicId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/course-offerings/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/courses/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/exams/:examId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/exams/:examId/courses/:examCourseId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/exams/:examId/scores/:scoreId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/exams/:examId/status` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/grades/:gradeId/finalize` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/grading-policies/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/promotion-rules/:ruleId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/result-cards/:cardId/conduct` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/result-cards/:cardId/publish` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/result-cards/:cardId/remark` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/schools/:schoolId/years/:yearId/students/:studentId/enrollment` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/section-attendance/:date/:sectionId/:studentId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/sections/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/students/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /academics/students/:id/descriptors` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/attendance` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/attendance/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/attendance/daily/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/classwork` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/classwork/topics` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/course-offerings` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/courses` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/enrollments` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/exams` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/exams/:examId/courses` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/exams/:examId/scores` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/exams/:examId/scores/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/grades/finalize/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/grades/record` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/grades/record/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/grading-policies` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/promotion-rules` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/schools/:schoolId/academic-years/:fromAyId/promote-from` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/schools/:schoolId/academic-years/:fromAyId/promote-from/:targetAyId/commit` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/schools/:schoolId/years/:yearId/enrollments/close-year` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/schools/:schoolId/years/:yearId/students/:studentId/no-show` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/schools/:schoolId/years/:yearId/students/:studentId/transfer` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/schools/:schoolId/years/:yearId/students/:studentId/withdraw` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/section-attendance` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/section-attendance/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/sections` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/sections/:id/homeroom-students` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/sections/:id/homeroom-students/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/sections/:id/students` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/sections/homeroom` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/students` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/students/:id/link-guardian` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/students/check-duplicate` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/students/import` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /academics/students/import/iemis` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |

### finance

| Route | Class | Authz | Guards |
|---|---|---|---|
| `DELETE /finance/schools/:schoolId/credit-notes/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /finance/schools/:schoolId/discount-rules/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /finance/schools/:schoolId/fee-structures/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/payments/:paymentId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/payments/:paymentId/receipt` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/payments/:paymentId/receipt/pdf` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/payments/verify/:sessionId` | authn-only | — | JwtAuthGuard |
| `GET /finance/schools/:schoolId/credit-notes` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/credit-notes/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/dashboard/summary` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/discount-rules` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/discount-rules/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/fee-structures` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/fee-structures/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/fee-structures/:id/overrides` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/fee-structures/:id/versions` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/invoices` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/invoices/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/invoices/:id/pdf` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/invoices/:invoiceId/payments` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/invoices/export` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/payment-gateways` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/payment-gateways/admin` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/payments` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/payments/export` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/refunds` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/refunds/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/student-accounts` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/student-accounts/:accountId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /finance/schools/:schoolId/student-accounts/:accountId/ledger` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /finance/schools/:schoolId/discount-rules/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /finance/schools/:schoolId/fee-structures/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /finance/schools/:schoolId/invoices/:id` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/credit-notes` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/credit-notes/:id/apply` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/discount-rules` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/fee-structures` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/invoices` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/invoices/:id/issue` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/invoices/bulk-generate` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/invoices/bulk-issue` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/payments/:paymentId/reconcile` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/payments/:paymentId/refund` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/payments/:paymentId/void` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/payments/initiate` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/payments/manual` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/refunds` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/refunds/:id/approve` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/refunds/:id/complete` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/refunds/:id/process` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /finance/schools/:schoolId/refunds/:id/reject` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /internal/webhooks/enrollment-completed` | internal | — | InternalApiKeyGuard |
| `POST /internal/webhooks/student-withdrawn` | internal | — | InternalApiKeyGuard |
| `PUT /finance/schools/:schoolId/payment-gateways/:gateway` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |

### identity

| Route | Class | Authz | Guards |
|---|---|---|---|
| `DELETE /calendar-blocks/:blockId` | authn-only | — | JwtAuthGuard |
| `DELETE /education-organizations/escs/:escId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `DELETE /education-organizations/leas/:leaId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `DELETE /education-organizations/networks/:networkId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `DELETE /education-organizations/networks/:networkId/members/:memberId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `DELETE /schools/:schoolId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `DELETE /schools/:schoolId/academic-sessions/:sessionId` | authn-only | — | JwtAuthGuard |
| `DELETE /schools/:schoolId/academic-years/:yearId/holidays/:holidayId` | authn-only | — | JwtAuthGuard |
| `DELETE /schools/:schoolId/bell-schedules/:bellScheduleId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /schools/:schoolId/class-periods/:periodId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /schools/:schoolId/departments/:departmentId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `DELETE /schools/:schoolId/locations/:locationId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `DELETE /sessions/:id` | authn-only | — | JwtAuthGuard |
| `DELETE /staff/:staffId` | authn-only | — | JwtAuthGuard |
| `DELETE /staff/:staffId/assignments/:assignmentId` | authn-only | — | JwtAuthGuard |
| `DELETE /staff/:staffId/credentials/:credentialId` | authn-only | — | JwtAuthGuard |
| `DELETE /staff/:staffId/trainings/:trainingId` | authn-only | — | JwtAuthGuard |
| `DELETE /users/:id` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `DELETE /users/:id/roles/:schoolId` | authn-only | — | JwtAuthGuard |
| `DELETE /users/:userId/security/sessions/:sessionId` | authn-only | — | JwtAuthGuard |
| `GET /archetype-defaults` | authn-only | — | JwtAuthGuard |
| `GET /auth/health` | public | — | — |
| `GET /auth/me` | authn-only | — | JwtAuthGuard |
| `GET /calendar-blocks` | authn-only | — | JwtAuthGuard |
| `GET /calendar-blocks/:blockId` | authn-only | — | JwtAuthGuard |
| `GET /credentials/expiring` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/escs` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/escs/:escId` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/hierarchy` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/leas` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/leas/:leaId` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/networks` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/networks/:networkId` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/networks/:networkId/members` | authn-only | — | JwtAuthGuard |
| `GET /education-organizations/sea` | authn-only | — | JwtAuthGuard |
| `GET /holiday-seeds` | authn-only | — | JwtAuthGuard |
| `GET /iemis/audit` | authz | RequireIemisPermission | JwtAuthGuard, IemisPermissionGuard |
| `GET /iemis/verify-school-code/:code` | authz | RequireIemisPermission | JwtAuthGuard, IemisPermissionGuard |
| `GET /reporting/snapshots` | authn-only | — | JwtAuthGuard |
| `GET /reporting/snapshots/:snapshotId` | authn-only | — | JwtAuthGuard |
| `GET /reporting/snapshots/:snapshotId/download` | authn-only | — | JwtAuthGuard |
| `GET /school-years` | authn-only | — | JwtAuthGuard |
| `GET /school-years/current` | authn-only | — | JwtAuthGuard |
| `GET /school-years/current-all` | authn-only | — | JwtAuthGuard |
| `GET /schools` | authn-only | — | JwtAuthGuard, GlobalRoleGuard |
| `GET /schools/:schoolId` | authn-only | — | JwtAuthGuard, GlobalRoleGuard |
| `GET /schools/:schoolId/academic-sessions` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/academic-sessions/:sessionId` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/academic-years` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/academic-years/:yearId` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/academic-years/:yearId/grading-periods` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/academic-years/:yearId/holidays` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/academic-years/current` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/activation-requirements` | authn-only | — | JwtAuthGuard, GlobalRoleGuard |
| `GET /schools/:schoolId/audit-log` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `GET /schools/:schoolId/bell-schedules` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/bell-schedules/:bellScheduleId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/branding` | authn-only | — | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/calendar-dates` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/calendar-dates/:date` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/calendar-dates/stats` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/calendars` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/calendars/:calendarId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/class-periods` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/class-periods/:periodId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/configuration` | authn-only | — | JwtAuthGuard, GlobalRoleGuard |
| `GET /schools/:schoolId/departments` | authn-only | — | JwtAuthGuard, GlobalRoleGuard |
| `GET /schools/:schoolId/departments/:departmentId` | authn-only | — | JwtAuthGuard, GlobalRoleGuard |
| `GET /schools/:schoolId/holidays` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/locations` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/locations/:locationId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/pdf-templates/:docType/current` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `GET /schools/:schoolId/shift-profile` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/staff` | authn-only | — | JwtAuthGuard |
| `GET /schools/:schoolId/users` | authn-only | — | JwtAuthGuard |
| `GET /sessions` | authn-only | — | JwtAuthGuard |
| `GET /sessions/:id` | authn-only | — | JwtAuthGuard |
| `GET /sessions/user/:userId` | authn-only | — | JwtAuthGuard |
| `GET /staff` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/assignments` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/assignments/:assignmentId` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/credentials` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/credentials/:credentialId` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/employment-history` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/leave` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/leave/:leaveId` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/trainings` | authn-only | — | JwtAuthGuard |
| `GET /staff/:staffId/trainings/:trainingId` | authn-only | — | JwtAuthGuard |
| `GET /staff/by-email` | authn-only | — | JwtAuthGuard |
| `GET /staff/search/:term` | authn-only | — | JwtAuthGuard |
| `GET /tenants/:tenantId` | authn-only | — | JwtAuthGuard |
| `GET /tenants/:tenantId/settings` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `GET /tenants/lookup` | public | — | — |
| `GET /tenants/my/settings` | authn-only | — | JwtAuthGuard |
| `GET /users` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `GET /users/:id` | authn-only | — | JwtAuthGuard |
| `GET /users/:id/assignments` | authn-only | — | JwtAuthGuard |
| `GET /users/:id/display-name` | authn-only | — | JwtAuthGuard |
| `GET /users/:id/preferences` | authn-only | — | JwtAuthGuard |
| `GET /users/:id/roles` | authn-only | — | JwtAuthGuard |
| `GET /users/:id/roles/:schoolId` | authn-only | — | JwtAuthGuard |
| `GET /users/:id/roles/permissions/catalog` | authn-only | — | JwtAuthGuard |
| `GET /users/:userId/security` | authn-only | — | JwtAuthGuard |
| `GET /users/:userId/security/login-history` | authn-only | — | JwtAuthGuard |
| `GET /users/:userId/security/sessions` | authn-only | — | JwtAuthGuard |
| `GET /users/me` | authn-only | — | JwtAuthGuard |
| `GET /users/me/permissions` | authn-only | — | JwtAuthGuard |
| `PATCH /calendar-blocks/:blockId` | authn-only | — | JwtAuthGuard |
| `PATCH /education-organizations/escs/:escId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /education-organizations/leas/:leaId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /education-organizations/networks/:networkId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /education-organizations/networks/:networkId/members/:memberId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /reporting/snapshots/:snapshotId/transition` | authn-only | — | JwtAuthGuard |
| `PATCH /schools/:schoolId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /schools/:schoolId/academic-sessions/:sessionId` | authn-only | — | JwtAuthGuard |
| `PATCH /schools/:schoolId/bell-schedules/:bellScheduleId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /schools/:schoolId/branding` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /schools/:schoolId/calendar-dates/:date` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /schools/:schoolId/calendars/:calendarId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /schools/:schoolId/class-periods/:periodId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /schools/:schoolId/configuration` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /schools/:schoolId/departments/:departmentId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /schools/:schoolId/grade-levels` | authz | RequirePermission | JwtAuthGuard, GlobalRoleGuard, PermissionGuard |
| `PATCH /schools/:schoolId/locations/:locationId` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `PATCH /schools/:schoolId/status` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /staff/:staffId` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/assignments/:assignmentId` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/credentials/:credentialId` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/credentials/:credentialId/verify` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/employment-status` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/leave/:leaveId/approve` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/leave/:leaveId/cancel` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/leave/:leaveId/reject` | authn-only | — | JwtAuthGuard |
| `PATCH /staff/:staffId/trainings/:trainingId` | authn-only | — | JwtAuthGuard |
| `PATCH /tenants/:tenantId` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /tenants/:tenantId/settings` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /tenants/:tenantId/settings/confirm` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /users/:id` | authn-only | — | JwtAuthGuard |
| `PATCH /users/:id/global-role` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PATCH /users/:id/preferences` | authn-only | — | JwtAuthGuard |
| `PATCH /users/:id/roles/:schoolId` | authn-only | — | JwtAuthGuard |
| `POST /admin/cleanup-expired-roles` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /auth/login` | public | — | — |
| `POST /auth/logout` | authn-only | — | JwtAuthGuard |
| `POST /auth/refresh` | authn-only | — | JwtAuthGuard |
| `POST /calendar-blocks` | authn-only | — | JwtAuthGuard |
| `POST /education-organizations/escs` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /education-organizations/leas` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /education-organizations/networks` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /education-organizations/networks/:networkId/members` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /reporting/snapshots` | authn-only | — | JwtAuthGuard |
| `POST /reporting/snapshots/preflight` | authn-only | — | JwtAuthGuard |
| `POST /roles/backfill-from-staff` | authz | RequireGlobalRole | JwtAuthGuard |
| `POST /schools` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /schools/:schoolId/academic-sessions` | authn-only | — | JwtAuthGuard |
| `POST /schools/:schoolId/academic-years` | authn-only | — | JwtAuthGuard |
| `POST /schools/:schoolId/academic-years/:yearId/generate-calendar` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/academic-years/:yearId/grading-periods` | authn-only | — | JwtAuthGuard |
| `POST /schools/:schoolId/academic-years/:yearId/holidays` | authn-only | — | JwtAuthGuard |
| `POST /schools/:schoolId/bell-schedules` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/bell-schedules/:bellScheduleId/set-default` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/bell-schedules/preset` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/branding/assets/upload-url` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/calendar-dates` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/calendar-dates/bulk` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/calendar-dates/bulk-update` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/calendars` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/class-periods` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/departments` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /schools/:schoolId/locations` | authz | RequirePermission | JwtAuthGuard, PermissionGuard |
| `POST /schools/:schoolId/staff` | authn-only | — | JwtAuthGuard |
| `POST /sessions/revoke-all` | authn-only | — | JwtAuthGuard |
| `POST /sessions/user/:userId/revoke-all` | authn-only | — | JwtAuthGuard |
| `POST /staff` | authn-only | — | JwtAuthGuard |
| `POST /staff/:staffId/assignments` | authn-only | — | JwtAuthGuard |
| `POST /staff/:staffId/credentials` | authn-only | — | JwtAuthGuard |
| `POST /staff/:staffId/leave` | authn-only | — | JwtAuthGuard |
| `POST /staff/:staffId/trainings` | authn-only | — | JwtAuthGuard |
| `POST /staff/with-user` | authn-only | — | JwtAuthGuard |
| `POST /tenants/:tenantId/onboarding/complete` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /users` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /users/:id/roles` | authn-only | — | JwtAuthGuard |
| `POST /users/:id/roles/:schoolId/change` | authn-only | — | JwtAuthGuard |
| `POST /users/:id/roles/permissions/check` | authn-only | — | JwtAuthGuard |
| `POST /users/:userId/security/change-password` | authn-only | — | JwtAuthGuard |
| `POST /users/:userId/security/mfa/disable` | authn-only | — | JwtAuthGuard |
| `POST /users/:userId/security/mfa/setup` | authn-only | — | JwtAuthGuard |
| `POST /users/:userId/security/mfa/verify` | authn-only | — | JwtAuthGuard |
| `POST /users/:userId/security/sessions/revoke-all` | authn-only | — | JwtAuthGuard |
| `POST /users/parent-accounts` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `POST /users/student-accounts` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PUT /education-organizations/sea` | authz | RequireGlobalRole | JwtAuthGuard, GlobalRoleGuard |
| `PUT /schools/:schoolId/academic-years/:yearId` | authn-only | — | JwtAuthGuard |
| `PUT /schools/:schoolId/academic-years/:yearId/grading-periods/:termId` | authn-only | — | JwtAuthGuard |
| `PUT /schools/:schoolId/academic-years/:yearId/set-current` | authn-only | — | JwtAuthGuard |
| `PUT /schools/:schoolId/academic-years/:yearId/status` | authn-only | — | JwtAuthGuard |
