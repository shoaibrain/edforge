# EdForge — Engineering conventions

The canonical engineering conventions, deploy ladder, dependency pins,
archetype rules, and edit-trap catalogue for this repo live in a single source
of truth: **[AGENTS.md](AGENTS.md)**. It is shared by every coding agent that
works this codebase, so there is one set of rules to maintain, not two.

Claude Code loads that content automatically via the import below — read
[AGENTS.md](AGENTS.md) directly for the full text.

@AGENTS.md
