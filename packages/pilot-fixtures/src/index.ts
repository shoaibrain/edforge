/**
 * @edforge/pilot-fixtures — public API.
 *
 * Sprint C1.0 ships the registry skeleton (`listPilots`, `loadPilot`)
 * and the metadata type system. C1.1–C1.7 add the per-pilot fixture
 * data and the parametric loader utility (`loadPilotFixture`).
 */

export * from './types';
export * from './pilot-registry';
