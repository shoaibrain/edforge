name: deploy-evidence-hygiene

# Prevent raw deploy logs, smoke transcripts, and generated CSV evidence from
# being force-added under docs/deploys. The only tracked deploy evidence file is
# the sanitized INDEX.md unless this gate is intentionally changed in a future PR.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  deploy-evidence-hygiene:
    name: deploy-evidence-hygiene
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Check tracked deploy evidence
        run: npm run lint:deploy-evidence
